paths:

  /.well-known/oauth-protected-resource:
    get:
      tags: [Auth]
      summary: Publica os metadados OAuth do Resource Server
      description: Metadados públicos de Protected Resource conforme RFC 9728.
      operationId: oauthProtectedResourceMetadata
      security: []
      responses:
        '200':
          description: Resource, Authorization Server e scopes públicos suportados.
          content:
            application/json:
              schema:
                type: object
                required: [resource, authorization_servers]
                properties:
                  resource: { type: string, format: uri }
                  authorization_servers:
                    type: array
                    items: { type: string, format: uri }
                  scopes_supported:
                    type: array
                    items: { type: string }

  # ── Health / WebSocket ─────────────────────────────────────────────────────

  /v1.0/health-check:
    get:
      tags: [Health]
      summary: Verifica a saúde da API e das dependências
      description: |
        Única rota pública (sem JWT). Verifica cache, DynamoDB e S3.
        Retorna `200` quando tudo está `pass`, `207` em degradação parcial e
        `503` quando alguma dependência crítica falhou.
      operationId: healthCheck
      security: []
      responses:
        '200':
          description: Todas as dependências saudáveis
          content:
            application/health+json:
              schema: { $ref: '#/components/schemas/Health' }
        '207':
          description: Degradação parcial
          content:
            application/health+json:
              schema: { $ref: '#/components/schemas/Health' }
        '503':
          description: Dependência crítica indisponível
          content:
            application/health+json:
              schema: { $ref: '#/components/schemas/Health' }

  /v1.0/ws:
    get:
      tags: [Health]
      summary: Canal WebSocket de eventos do documento
      description: |
        Upgrade para WebSocket. O resultado da emissão (autorização/rejeição da
        SEFAZ) é empurrado por aqui, evitando polling.

        O JWT **não** vai na query string (ficaria em log de proxy): após o
        upgrade, o cliente envia o token como primeiro frame de texto. O
        servidor valida, confirma a associação do usuário à organização e só
        então passa a entregar eventos.
      operationId: connectWebSocket
      security: []
      parameters:
        - name: org_pk
          in: query
          required: true
          description: PK da organização cujos eventos serão recebidos.
          schema: { type: string }
      responses:
        '101':
          description: Conexão promovida a WebSocket
        '400':
          description: '`org_pk` ausente ou origem não permitida'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  # ── Distributions (NF-e / CT-e / MDF-e / NFS-e) ───────────────────────────

  /v1.0/distributions/{doc_type}/history:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
      - $ref: '#/components/parameters/DocType'
    get:
      tags: [Distributions]
      summary: Lista os documentos distribuídos
      operationId: listDistributions
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Página de documentos distribuídos
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Page'
                  - type: object
                    properties:
                      items:
                        type: array
                        items: { $ref: '#/components/schemas/Distribution' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /v1.0/distributions/{doc_type}/sync:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
      - $ref: '#/components/parameters/DocType'
    post:
      tags: [Distributions]
      summary: Dispara a sincronização com a SEFAZ ou o ADN
      description: |
        Enfileira a busca a partir do último NSU conhecido. NF-e, CT-e e MDF-e
        consultam a SEFAZ; NFS-e consulta o Ambiente de Dados Nacional (ADN).
        A API aplica a janela de consulta antes de enfileirar.
      operationId: syncDistributions
      responses:
        '202':
          description: Sincronização enfileirada
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /v1.0/distributions/{doc_type}/history/{nsu}/xml:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
      - $ref: '#/components/parameters/DocType'
      - $ref: '#/components/parameters/Nsu'
    get:
      tags: [Distributions]
      summary: Baixa o XML de um NSU
      operationId: getDistributionXml
      responses:
        '200': { $ref: '#/components/responses/XmlDownload' }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /v1.0/distributions/{doc_type}/nsu/{nsu}:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
      - $ref: '#/components/parameters/DocType'
      - $ref: '#/components/parameters/Nsu'
    get:
      tags: [Distributions]
      summary: Detalha um documento distribuído pelo NSU
      operationId: getDistributionByNsu
      responses:
        '200':
          description: Documento distribuído
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Distribution' }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /v1.0/distributions/nfe/key:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
    post:
      tags: [Distributions]
      summary: Enfileira uma consChNFe manual por chave de acesso (NF-e)
      description: |
        Força uma nova consulta de distribuição por chave de acesso — usada
        quando um Ciência automático (resNFe) nunca evoluiu para o documento
        completo. Assíncrono: consome a mesma janela de 20 consultas/hora do
        consNSU/consChNFe. NF-e apenas — CT-e/MDF-e não têm o conceito de
        resNFe/Ciência do destinatário que motiva uma reconsulta manual.
      operationId: importNfeByKey
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [access_key]
              properties:
                access_key:
                  type: string
                  description: Chave de acesso de 44 dígitos da NF-e.
      responses:
        '202':
          description: Consulta enfileirada
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /v1.0/distributions/{doc_type}/import-xml:
    parameters:
      - $ref: '#/components/parameters/OrgHeader'
      - $ref: '#/components/parameters/DocType'
    post:
      tags: [Distributions]
      summary: Importa uma NF-e/NFC-e a partir de um arquivo XML
      description: |
        Aceita `nfeProc` (com protocolo) ou `NFe` (sem protocolo, apenas
        assinado). Assíncrono: valida tamanho/raiz, faz staging em S3 e
        enfileira o job `import_xml`. O worker classifica o vínculo com a
        organização (emit > dest > transp), confirma junto à SEFAZ
        (consulta protocolo) e persiste como se o documento tivesse chegado
        pela distribuição — resultado via WebSocket
        (`new_distribution_nfe` ou `import_xml_failed`). `doc_type` restrito
        a `nfe`/`nfce`.
      operationId: importXml
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Arquivo XML da NF-e/NFC-e (`nfeProc` ou `NFe`), até 1 MiB.
      responses:
        '202':
          description: Importação enfileirada
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '413':
          description: Arquivo XML excede o limite de tamanho permitido
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '429':
          $ref: '#/components/responses/TooManyRequests'

  # ── Audit logs ─────────────────────────────────────────────────────────────

  /v1.0/audit-logs:
    get:
      tags: [Audit Logs]
      summary: Lista a trilha de auditoria da organização
      description: Restrito a OWNER e ADMIN.
      operationId: listAuditLogs
      parameters:
        - $ref: '#/components/parameters/OrgHeader'
        - name: resource_type
          in: query
          description: 'Filtra por tipo de recurso (ex.: `products`, `organizations`).'
          schema: { type: string }
        - name: resource_id
          in: query
          description: Filtra por identificador do recurso.
          schema: { type: string }
        - name: user_id
          in: query
          description: Filtra pelo autor da mutação.
          schema: { type: string }
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Página de registros de auditoria
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Page'
                  - type: object
                    properties:
                      items:
                        type: array
                        items: { $ref: '#/components/schemas/AuditLog' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  # ── External ───────────────────────────────────────────────────────────────

  /v1.0/external/lookup-organizations:
    get:
      tags: [External]
      summary: Consulta cadastro de contribuinte na SEFAZ
      description: |
        Consulta o cadastro (situação, inscrição estadual, endereço) na SEFAZ da
        UF informada, usando o certificado da organização ativa. Serve para
        preencher o cadastro de pessoas sem digitação.
      operationId: lookupOrganization
      parameters:
        - $ref: '#/components/parameters/OrgHeader'
        - name: cpf_cnpj
          in: query
          required: true
          description: CPF ou CNPJ a consultar, apenas dígitos.
          schema: { $ref: '#/components/schemas/CpfCnpj' }
        - name: uf
          in: query
          required: true
          description: UF cuja SEFAZ será consultada.
          schema: { $ref: '#/components/schemas/Uf' }
      responses:
        '200':
          description: Dados cadastrais retornados pela SEFAZ
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        '400':
          $ref: '#/components/responses/BadRequest'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
