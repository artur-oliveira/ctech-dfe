# Included inside `location /` by the shared setup-nginx.sh. These cannot live in
# http {}: a location that declares any limit_req stops inheriting the
# http-level ones.
limit_req zone=req_by_tenant burst=1000 nodelay;
limit_conn conn_by_tenant 500;
